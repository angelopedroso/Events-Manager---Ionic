import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'gerenciador-eventos',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
